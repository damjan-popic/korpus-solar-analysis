# Raw input

Place the original Šolar CoNLL-U here:

```text
data/raw/solar-orig.conllu
```

The reannotation script preserves original `# sent_id` values such as `solar1.1.1` so the corpus can be joined to `data/metadata/solar-meta.tsv`.
