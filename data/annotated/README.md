# Annotated output

The pipeline writes the CLASSLA-reannotated corpus here, normally:

```text
data/annotated/solar-classla.conllu
```

This directory is kept in the repo only as a target location; large generated corpora can be excluded from version control.
